apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  type: NodePort # 映射到物理机
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000 # 映射到物理机

# 更新 svc kubectl edit svc/nginx-service